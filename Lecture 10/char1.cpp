#include<bits/stdc++.h>
using namespace std;
// const int N = ;

int main() {
	char p;
	/*cin >> p;
	cout << p;*/
	// p = cin.get();

	// p = cin.get();
	// cin.get(p);
	while (cin.get(p)) {
		if (p >= 97 and p <= 122) {
			p = p - 32;
		}
		cout << p;
	}
	//cout << "mayak";
}