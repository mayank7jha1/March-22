#include<bits/stdc++.h>
using namespace std;
// const int N = 0;

int main() {
	/*
		Bubble sort
		Selection sort
		Insertion sort
	*/

	// STL: Standard Template Library.

	//sort();

	/*	int a[] = {12, 3, 4, 5, 9, 7, 8, 5, 2};
		sort(a, a + 9);
		for (auto x : a) {
			cout << x << " ";
		}
		cout << endl;
	*/
	/*string s;
	cin >> s;

		cout << s.length() << endl;
		cout << *s.begin() << endl;
	//cout << *s.end() << endl;

	sort(s.begin(), s.end());
	cout << s << endl;*/

	/*int a = 10;
	int b = 20;
	cout << a + b << endl;
	string s, t;
	cin >> s >> t;
	string m = s + t;
	cout << m << endl;

	if (s > t) {
		cout << "Yes";
	} else {
		cout << "NO" << endl;
	}

	*/


}