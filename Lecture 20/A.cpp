#include<bits/stdc++.h>
using namespace std;
int main() {
	int test;
	cin >> test;
	// cout << "Mayank";
	//cin.ignore();
	while (test--) {
		string s, t;
		//cin.ignore();
		cin >> s;
		// cin.ignore(numeric_limits<streamsize>::max(), '\n');
		// getline(cin, s);
		//cout << s << endl;
		/*	for (int i = 0; i < s.length(); i++)
			{
				if (isalnum(s[i]))
				{
					t.push_back(s[i]);
				}
			}

			int st = 0, e = t.length() - 1;
			int flag = 1;
			while (st <= e)
			{
				t[st] = tolower(t[st]);
				t[e] = tolower(t[e]);
				if (t[st] != t[e])
				{
					flag = 0;
					break;
				}
				st++;
				e--;
			}
			if (flag == 1)
			{
				cout << "Yes" << endl;
			}
			else
			{
				cout << "No" << endl;
			}*/
		//cin.ignore();
	}
}